# ParkSimulator

## Browser Run

1. Build browser bundle:
   - `npm.cmd run web:build`
2. Start local static server:
   - `npm.cmd run web:run`
3. Open:
   - `http://localhost:5173`

Or one command directly:
- `npm.cmd run web:run`
